/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      fontFamily: {
        theriven: ['theriven', 'sans-serif'], // Tambahkan custom font "Theriven"
      },
    },
  },
  plugins: [],
}
